# Archha Grocery Backend

Node/Express API structure for the grocery ecommerce website.

## Local usage

1. Run `npm install`
2. Copy `.env.example` to `.env`
3. Run `npm run dev`

The frontend can still run as static HTML when the backend is not active.
